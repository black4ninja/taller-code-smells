// =============================================================
// C3 - Formatear direccion postal
// =============================================================
//
// Implementa: formatearDireccion(direccion)
//
// - direccion: { calle, numero, colonia, ciudad, estado, cp, pais }
//   (todos los campos son strings opcionales)
//
// Output: string con los campos NO vacios unidos por ", "
//   Ejemplo: "Av. Reforma 123, Centro, CDMX, CDMX 06000, Mexico"
//   Los campos vacios/undefined deben OMITIRSE (sin ",," ni "undefined").
//
// Restricciones que validan los tests:
//   - formatearDireccion recibe EXACTAMENTE 1 parametro (Parameter Object)
//   - <= 8 lineas
//   - maximo 2 "if"
//   - PROHIBIDO usar "+=" para concatenar strings
//
// Hint pedagogico: el junior recibe 7 strings primitivos y concatena con
// result += ... if(...). El refactor limpio agrupa con filter(Boolean).join().
// =============================================================

function formatearDireccion(direccion) {
  // TODO: implementar
  return '';
}

module.exports = { formatearDireccion };
